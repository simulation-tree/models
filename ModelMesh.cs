using Simulation;

namespace Models
{
    public readonly struct ModelMesh
    {
        public readonly EntityID value;

        public ModelMesh(EntityID value)
        {
            this.value = value;
        }
    }
}
