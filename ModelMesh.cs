using Simulation;

namespace Models
{
    public readonly struct ModelMesh
    {
        public readonly rint value;

        public ModelMesh(rint value)
        {
            this.value = value;
        }
    }
}
