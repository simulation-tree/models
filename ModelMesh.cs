using Simulation;

namespace Models
{
    public readonly struct ModelMesh
    {
        public readonly eint value;

        public ModelMesh(eint value)
        {
            this.value = value;
        }
    }
}
